<h2>This is an index placeholder</h2>
<% @jobs.each do |job| %>
  <h3><%= job.title %></h3>  
  <p><%= truncate(job.details, length: 100)%></p>
  <p> <%= job.close_date %> </p>
  <p> <%= link_to 'More...', job %> </p>
  <p> <%= link_to 'New Posting', new_job_path() %> </p>
<% end %>

